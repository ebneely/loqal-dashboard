/** The route file, and nothing else. This screen reads no search params. */
import { SettingsScreen } from "./settings-screen";

export default function AdminSettingsPage() {
  return <SettingsScreen />;
}
