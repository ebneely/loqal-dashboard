# Dependencies — reinstallable from the lockfile.
node_modules/

# Next.js build output
.next/
out/
next-env.d.ts

# Test and coverage output
coverage/
*.tsbuildinfo

# Logs
*.log
npm-debug.log*

# OS / editors
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/settings.json

# Local env. Unlike the backend — which commits its .env deliberately — nothing
# here needs a secret to run, so there is no reason to carry one.
.env
.env.local
.env*.local
